import type { MarkdownPage } from "../../../lib/content";

/**
 * Props for a Reference hub or working page loaded from `website/*.md`.
 */
export type ReferencePageProps = {
  page: MarkdownPage;
};
